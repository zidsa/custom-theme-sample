<div class="products-title-section" {% if background_image %} style="background-image: url('{{ background_image }}')" {% endif %}>
    <div class="container d-flex align-items-center">
        <h1>{{ title }}</h1>
        <p>{{ description }}</p>
    </div>
</div>

